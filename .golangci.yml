version: "2"

run:
  modules-download-mode: readonly

linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gofmt
    - goimports
    - misspell
  settings:
    errcheck:
      check-type-assertions: true
    govet:
      check-shadowing: false
    gofmt:
      simplify: true
    misspell:
      locale: US
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck
